BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[PlayerGameStat] ADD [score] INT NOT NULL CONSTRAINT [PlayerGameStat_score_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
